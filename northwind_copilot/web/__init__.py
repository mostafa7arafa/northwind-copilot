"""Web layer: the FastAPI service the frontend talks to.

Dynamic model selection (local Ollama, OpenAI, or OpenRouter), a streaming chat
endpoint that emits structured pipeline events, and user-preference persistence.
"""
